
midi piano = 0, 255

melody x piano { C } ionian { 1 2 3 4 5 6 7 8 }
rhythm  h (4 / 4) 1 |: 8 8 8 8 8 8 8 8 :|

composition main =
   ({C } ionian x) <- h;
   ({C#} ionian x) <- h;
   ({D } ionian x) <- h;
   ({D#} ionian x) <- h;
   ({E } ionian x) <- h;
   ({F } ionian x) <- h;
   ({F#} ionian x) <- h;
   ({G } ionian x) <- h;
   ({G#} ionian x) <- h;
   ({A } ionian x) <- h;
   ({A#} ionian x) <- h;
   ({B } ionian x) <- h



